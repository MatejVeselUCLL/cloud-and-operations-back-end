name: Publish Docker image

on:
  push:
    branches: [ dev ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: get-pom-version
        id: pom-version
        uses: PERES-Richard/maven-get-version-action@v3.0.0
        with:
          path: "../"  # Optional: pom.xml relative folder path

      - name: Print Maven POM project version
        run: echo "version = ${{ steps.pom-version.outputs.version }}"

      - name: Log in to GHCR
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Docker image
        run: docker build -t ghcr.io/${{ github.repository_owner }}/demo-cloud-and-operations-back-end-dev:${{ steps.pom-version.outputs.version }} .

      - name: Push Docker image
        run: docker push ghcr.io/${{ github.repository_owner }}/demo-cloud-and-operations-back-end-dev:${{ steps.pom-version.outputs.version }}